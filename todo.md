Done (v55):
✅ 实现中文键盘的"分词键"
   - 键盘布局使用 composing 属性：`{click: key_fenci, composing: "'"}`
   - preset_keys 中定义默认发送为 Eisu_toggle（无操作）
   - 效果：无候选时按分词键不输入任何东西，有候选时发送 `'` 分词
--------
暂时不做:

### 1. 精确输入排除模糊候选（预计 2-4 小时）
**问题描述**：
- WE 共键有三种操作：点击（模糊匹配 W/E）、左滑（精确 W）、右滑（精确 E）
- 当前滑动精确输入 `ee` 时，仍会出现 `ww` 的模糊候选

**技术限制**：
- Rime 的 `speller/algebra` 的 `derive` 规则作用于词库拼音，不是用户输入
- 无论点击还是滑动，发送给引擎的都是同一个字符，无法区分输入来源
- `derive` 创建等价变体，无法区分优先级

**Lua 解决方案**：
1. **键盘层**：滑动发送大写字母（如 `E`）作为"精确输入标记"
2. **Lua Processor**（~50-80行）：
   - 拦截大写字母输入
   - 记录到"精确输入序列"
   - 转换为小写字母发送给 speller
3. **Lua Filter**（~60-100行）：
   - 读取"精确输入序列"
   - 对比候选项的拼音与用户实际输入
   - 过滤掉不精确匹配的候选（如输入 `EE` 时过滤 `ww` 候选）
4. **配置集成**：在 schema 中注册 processor 和 filter

### 2. 底部功能键栏
参考简纯, 这个键盘在最下方有一行功能键, 左侧是切换系统输入法, 第二个是左键 第三个是麦克风(不知道什么作用), 第四个是右键, 第五个是收起键盘

### 3. 智能返回上级键盘
目前进入次级键盘后不能自动判断上级, 返回只能返回default, 现在的方案是分别定义不同的sub keyboard, 例如number_cn number_en, 里面只有返回键指向的键盘不同
当前代码冗余, 需要修复


