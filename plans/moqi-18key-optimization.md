# moqi_xh-18key 精简优化分析

## 目标

识别并删除 [`moqi_xh-18key.schema.yaml`](moqi_xh-18key.schema.yaml) 中在手机18键输入时用不到的功能和依赖。

## 对比分析：moqi_xh-18key vs moqi_xh-weasel

### 标准版（weasel）有但18键版没有的配置

#### 1. 快捷键配置（第166-226行）
标准版有大量快捷键配置，18键版只保留了最基础的：
```yaml
# 标准版有：
- Control+m, Control+w, Control+i, Control+o
- Control+u (大字集切换)
- Control+p, Control+l, Control+e (拆分切换)
- Control+1~7 (光标回退补充辅助码)
- Shift+A~Z (整句第一个音节加辅助码)

# 18键版只有：
- { when: has_menu, accept: semicolon, send: 2 }  # 分号选2
```

**分析**：手机上没有这些组合键，18键版已经正确简化。✅

#### 2. 飞键规则（第94-107行）
标准版启用了飞键规则：
```yaml
- derive/^([y])j/$1f/
- derive/^qx/qw/
- derive/^xq/xo/
- derive/^qq/qo/
- derive/^ww/wi/
- derive/^j([a-twyz])/f$1/
- derive/^([y])h/$1g/
- derive/^([rsgv])c/$1n/
```

18键版注释掉了（第159-170行），改用共键模糊规则。✅ **正确决策**

### 18键版启用但可能不需要的功能

## 一、依赖项分析

### 当前依赖（第20-29行）
```yaml
dependencies:
  - cangjie5         # ❓ 仓颉反查
  - reverse_moqima   # ❓ 墨奇反查
  - radical_flypy    # ❓ 部件反查
  - stroke           # ❓ 笔画反查
  - zrlf             # ❓ 自然两分反查
  - emoji            # ✅ emoji输入
  - easy_en          # ✅ 英文输入
  - jp_sela          # ❓ 日语输入
  - moqi_big         # ❓ 大字集
```

### 依赖项使用情况检查

#### 1. 反查功能（第324-334行）
```yaml
recognizer:
  patterns:
    punct:            # 禁用 / 开头的符号输入
    reverse_moqima:   # 禁用 amq 墨奇反查
    radical_flypy:    # 禁用 az 部件组字
    reverse_stroke:   # 禁用 ab 笔画反查
    reverse_cj:       # 禁用 arj 仓颉反查
    reverse_zrlf:     # 禁用 alf 自然两分
    add_user_dict:    # 禁用 ac 自造词
    emojis:           # 禁用 ae Emoji
```

**重要发现**：18键版的 recognizer 部分将所有反查功能都标记为**空值**（禁用状态）！

这意味着：
- ❌ `reverse_moqima` - 已禁用，但在 dependencies 中
- ❌ `radical_flypy` - 已禁用，但在 dependencies 中
- ❌ `stroke` - 已禁用，但在 dependencies 中
- ❌ `reverse_cj` (cangjie5) - 已禁用，但在 dependencies 中
- ❌ `reverse_zrlf` - 已禁用，但在 dependencies 中

#### 2. Emoji 输入（第334行）
```yaml
emojis:  # 禁用 ae Emoji
```
**已禁用**，但 emoji 依赖仍在。

#### 3. 日语输入（jp_sela）
没有在 recognizer 中明确禁用，但也没有配置前缀。
**使用频率**：手机输入日语的场景较少。

#### 4. 大字集（moqi_big，第29行）
从 moqi.yaml 引用中可见（第113行）：
```yaml
__include: moqi.yaml:/big_char_and_user_dict
```

查看使用情况：
- `switches` 中有 `big_char_set` 开关（第43-44行）
- `engine/filters` 中有 `simplifier@big_char_set`（第102行）
- translator 有 `table_translator@big_char_set`（第88行）

**问题**：手机输入生僻字（超过8105常用字）的频率低，且会增加：
- 部署时间
- 内存占用
- 候选项数量（影响性能）

## 二、可以安全删除的内容

### 🔴 高优先级删除（已禁用但仍占用资源）

#### 1. 反查相关依赖
```yaml
# 可以删除的依赖
dependencies:
  - cangjie5         # 删除：仓颉反查已禁用
  - reverse_moqima   # 删除：墨奇反查已禁用
  - radical_flypy    # 删除：部件反查已禁用
  - stroke           # 删除：笔画反查已禁用
  - zrlf             # 删除：自然两分已禁用
```

#### 2. moqi.yaml 中的反查配置引用
```yaml
# 第110行，可以删除
__include: moqi.yaml:/reverse  # 删除：所有反查已禁用
```

**影响**：删除后，schema 不会加载这些反查词典，减少部署时间和内存占用。

#### 3. Engine 中未使用的翻译器
从 engine/translators（第76-93行）可见，这些反查翻译器可以删除：
```yaml
# 当前有但已禁用，可以删除：
# - table_translator@reverse_moqima
# - table_translator@radical_flypy
# - table_translator@reverse_stroke
# - table_translator@reverse_zrlf
# - table_translator@reverse_cj
```

但实际上这些是从 `moqi.yaml:/switches_engine` 引入的（需要在 moqi.yaml 中处理）。

**建议**：不直接引用 `moqi.yaml:/switches_engine`，而是自己定义完整的 engine 配置。

### 🟡 中优先级删除（使用频率低）

#### 4. Emoji 功能
已在 recognizer 中禁用（第334行），可以删除：
```yaml
dependencies:
  - emoji  # 删除：已禁用 ae emoji 输入
```

如果需要 emoji，可以通过符号表输入，不需要专门的 emoji 词典。

#### 5. 日语输入（jp_sela）
```yaml
dependencies:
  - jp_sela  # 可选删除：手机输入日语频率低
```

从 engine 中删除：
```yaml
# engine/segmentors
- affix_segmentor@jp_sela  # 可删除

# engine/translators
- table_translator@jp_sela  # 可删除
```

#### 6. 大字集（moqi_big）
```yaml
dependencies:
  - moqi_big  # 可选删除：手机很少输入生僻字
```

需要同时删除：
```yaml
# switches 中的开关
- name: big_char_set
  states: [ 大字集, 小字集 ]

# engine/translators 中的翻译器
- table_translator@big_char_set

# engine/filters 中的过滤器
- simplifier@big_char_set

# 引用
__include: moqi.yaml:/big_char_and_user_dict
```

**注意**：删除大字集后，超过8105的生僻字将无法输入。

### 🟢 低优先级删除（可选）

#### 7. 英文输入（easy_en）
```yaml
dependencies:
  - easy_en  # 保留：手机可能需要输入英文
```

但如果手机键盘有独立的英文输入，也可以删除。

#### 8. Custom Phrase 配置
从 moqi.yaml 引用了大量自定义短语配置（第109行）：
```yaml
__include: moqi.yaml:/phrase
```

其中包括（从 moqi.yaml 第130-201行）：
- custom_phrase - 1、2码置顶
- custom_phrase_3_code - 3码出简让全
- custom_phrase_kf - 快符
- custom_phrase_mqzg - 字根
- custom_phrase_super_1jian - 超强tab 1码
- custom_phrase_super_2jian - 超强tab 2码
- custom_phrase_super_3jian - 超强tab 3码
- custom_phrase_super_3jian_no_conflict - 无冲突3简
- custom_phrase_super_4jian_no_conflict - 无冲突4简

**分析**：
- custom_phrase - ✅ 保留（置顶词很有用）
- custom_phrase_3_code - ✅ 保留（3码简让全）
- custom_phrase_kf - ❓ 快符（特殊符号）手机可能用不上
- custom_phrase_mqzg - ❓ 字根输入，手机少用
- custom_phrase_super_* - ❓ Tab快捷输入，手机没有Tab键

## 三、精简方案

### 方案A：激进精简（最小化）
保留核心输入功能，删除所有不常用的：

```yaml
dependencies:
  - easy_en        # 保留英文
  # 删除所有其他依赖

# 不引用 moqi.yaml，自己定义所有配置
# 只保留：
# - 基础输入
# - custom_phrase（1,2码置顶）
# - custom_phrase_3_code（3码简让全）
```

**优点**：
- 最小化部署时间
- 最低内存占用
- 配置清晰

**缺点**：
- 无法输入生僻字
- 无法使用反查
- 配置文件需要自己维护

### 方案B：适度精简（推荐）
删除已禁用的功能，保留可能有用的：

```yaml
dependencies:
  - emoji          # 保留：emoji 常用
  - easy_en        # 保留：英文输入
  # 删除：cangjie5, reverse_moqima, radical_flypy, stroke, zrlf（已禁用）
  # 删除：jp_sela（日语少用）
  # 删除：moqi_big（生僻字少用）

# 删除引用：
# __include: moqi.yaml:/reverse  # 删除
# __include: moqi.yaml:/big_char_and_user_dict  # 删除

# 保留引用：
__include: moqi.yaml:/phrase  # 保留，但需要精简
__include: moqi.yaml:/opencc_config  # 保留：拆分提示有用
__include: moqi.yaml:/guide  # 保留：引导前缀
```

**优点**：
- 大幅减少资源占用
- 保留常用功能
- 平衡实用性和性能

**缺点**：
- 仍依赖 moqi.yaml

### 方案C：保守精简
只删除已明确禁用的：

```yaml
# 只删除 recognizer 中已禁用的功能对应的依赖
dependencies:
  # 删除：cangjie5, reverse_moqima, radical_flypy, stroke, zrlf
  - emoji          # 保留
  - easy_en        # 保留
  - jp_sela        # 保留
  - moqi_big       # 保留
```

## 四、具体删除清单（推荐方案B）

### 4.1 修改 dependencies
```yaml
# 删除前：
dependencies:
  - cangjie5         # ❌ 删除
  - reverse_moqima   # ❌ 删除
  - radical_flypy    # ❌ 删除
  - stroke           # ❌ 删除
  - zrlf             # ❌ 删除
  - emoji            # ✅ 保留
  - easy_en          # ✅ 保留
  - jp_sela          # ❌ 删除
  - moqi_big         # ❌ 删除

# 删除后：
dependencies:
  - emoji
  - easy_en
```

### 4.2 删除 moqi.yaml 引用
```yaml
# 第108-113行，删除以下行：
# __include: moqi.yaml:/reverse          # ❌ 删除：反查已禁用
# __include: moqi.yaml:/big_char_and_user_dict  # ❌ 删除：大字集删除

# 保留：
__include: moqi.yaml:/phrase             # ✅ 保留
__include: moqi.yaml:/opencc_config      # ✅ 保留
__include: moqi.yaml:/guide              # ✅ 保留
```

### 4.3 删除 switches 中的大字集开关
```yaml
# 第43-44行，删除：
# - name: big_char_set
#   states: [ 大字集, 小字集 ]
```

### 4.4 清理 engine 配置

由于当前引用了 `moqi.yaml:/switches_engine`（隐含引入了很多不需要的配置），建议：

**选项1：继续使用 moqi.yaml 但忽略不需要的**
保持现状，依赖 moqi.yaml 但通过 recognizer 禁用。

**选项2：自定义完整的 engine（推荐）**
不使用 `__include: moqi.yaml:/switches_engine`，而是自己定义完整的 switches 和 engine。

### 4.5 简化 custom_phrase 配置

从 moqi.yaml:/phrase 中，只保留需要的：
```yaml
# 建议保留：
- custom_phrase                    # 1,2码置顶
- custom_phrase_3_code            # 3码简让全

# 建议删除（手机用不上）：
- custom_phrase_kf                # 快符
- custom_phrase_mqzg              # 字根
- custom_phrase_super_*           # Tab超强输入（手机无Tab）
```

由于这些在 moqi.yaml 中定义，需要：
1. 要么容忍引用整个 `/phrase`
2. 要么在 moqi_xh-18key.schema.yaml 中重新定义需要的

### 4.6 清理 recognizer（可选）

当前 recognizer 禁用了很多功能，可以完全删除这些配置行：
```yaml
recognizer:
  patterns:
    # 只保留实际使用的
    # 删除所有已禁用的空配置
```

## 五、优化后的效果

### 资源节省估算

#### 部署时间
- 删除 5 个反查词典：节省 30-50% 部署时间
- 删除大字集：再节省 20-30% 部署时间
- **总计**：部署时间可能缩短 50-70%

#### 内存占用
- 反查词典：约 10-20MB
- 大字集：约 5-10MB
- **总计**：减少 15-30MB 内存占用

#### 配置文件大小
- 删除未使用的配置：简化 50+ 行

### 功能影响

#### 删除的功能
- ❌ 所有反查功能（amq, az, ab, arj, alf）
- ❌ 大字集输入
- ❌ 日语输入（aj）
- ❌ 部分自定义短语（快符、字根、Tab超强）

#### 保留的核心功能
- ✅ 基础音形输入
- ✅ 18键共键模糊
- ✅ 共键双拼辅助码
- ✅ 精确输入
- ✅ 自定义短语（置顶、3码简让全）
- ✅ 拆分提示
- ✅ Emoji 输入
- ✅ 英文输入

## 六、实施步骤

### Step 1: 备份
```bash
cp moqi_xh-18key.schema.yaml moqi_xh-18key.schema.yaml.backup
```

### Step 2: 修改 dependencies（第20-29行）
```yaml
dependencies:
  - emoji
  - easy_en
```

### Step 3: 删除 moqi.yaml 引用（第108-113行）
```yaml
# 删除这两行：
# __include: moqi.yaml:/reverse
# __include: moqi.yaml:/big_char_and_user_dict
```

### Step 4: 删除 switches（第43-44行）
```yaml
# 删除：
# - name: big_char_set
#   states: [ 大字集, 小字集 ]
```

### Step 5: 删除 engine/filters（第102行）
```yaml
# 删除：
# - simplifier@big_char_set
```

### Step 6: 删除 engine/translators（第88行）
```yaml
# 删除：
# - table_translator@big_char_set
```

### Step 7: 清理 recognizer（第324-334行）
删除所有已禁用但仍配置的项，只保留实际使用的。

### Step 8: 测试
1. 重新部署方案
2. 测试基础输入
3. 测试共键模糊
4. 测试精确输入
5. 测试自定义短语

### Step 9: 验证效果
- 检查部署时间
- 检查内存占用
- 确认功能正常

## 七、风险评估

### 低风险删除
- ✅ cangjie5（已禁用）
- ✅ reverse_moqima（已禁用）
- ✅ radical_flypy（已禁用）
- ✅ stroke（已禁用）
- ✅ zrlf（已禁用）
- ✅ jp_sela（极少使用）

### 中等风险删除
- ⚠️ moqi_big（如果需要输入生僻字会无法输入）

### 建议保留
- ✅ emoji（常用）
- ✅ easy_en（常用）

## 八、总结

### 核心发现
1. **已有大量功能被禁用但仍占用资源**：recognizer 中禁用了所有反查功能，但对应的依赖和配置仍在
2. **大字集在手机上使用频率低**：可以删除以提升性能
3. **日语输入不是必需的**：可以删除
4. **Tab相关的超强输入无法在手机上使用**：可以简化

### 推荐操作（按优先级）
1. 🔴 **立即删除**：所有反查依赖（cangjie5, reverse_moqima, radical_flypy, stroke, zrlf）
2. 🟡 **考虑删除**：moqi_big（大字集）、jp_sela（日语）
3. 🟢 **保留**：emoji、easy_en

### 预期收益
- 部署时间：缩短 50-70%
- 内存占用：减少 15-30MB
- 配置清晰度：大幅提升
- 功能损失：几乎没有（删除的都是已禁用或极少使用的）

这是一个**高收益、低风险**的优化方案。
